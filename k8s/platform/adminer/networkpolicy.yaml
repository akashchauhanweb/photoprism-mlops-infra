apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-adminer
  namespace: photoprism-platform
spec:
  podSelector: { matchLabels: { app: adminer } }
  policyTypes: [Ingress]
  ingress:
    - ports:
        - protocol: TCP
          port: 8080
